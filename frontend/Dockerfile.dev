# syntax=docker/dockerfile:1.7
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci || npm install

# Source is mounted at runtime in compose for hot-reload.
EXPOSE 5173

CMD ["npm", "run", "dev"]
